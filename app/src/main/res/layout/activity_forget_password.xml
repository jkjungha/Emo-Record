<!--  forget_password_layout.xml  -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/bg_1"
    android:padding="16dp">
    <EditText android:id="@+id/resetEmailOrPhoneEditText" android:layout_width="369dp" android:layout_height="55dp" android:layout_marginBottom="16dp" android:background="@drawable/bg_text" android:hint=" 이메일 또는 전화번호 입력"/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/resetButton"
        android:layout_width="213dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_button"
        android:text="비밀번호 재설정"
        android:textColor="@color/white" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/backToLoginButton"
        android:layout_width="48dp"
        tools:ignore="SpeakableTextPresentCheck"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/back"
        android:backgroundTint="@color/green"
        android:textColor="@color/white" />
</LinearLayout>
